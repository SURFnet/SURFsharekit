class BasicDropdownItem {
    constructor(text, tooltipText, onClick) {
        this.text = text;
        this.tooltipText = tooltipText
        this.onClick = onClick
    }
}

export default BasicDropdownItem;