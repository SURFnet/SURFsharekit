version: '3.7'

services:
  surfsharekit-frontend:
    container_name: surfsharekit-frontend
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
    ports:
      - 3000:3000
    stdin_open: true