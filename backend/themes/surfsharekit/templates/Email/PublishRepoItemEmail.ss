<!DOCTYPE html>
<html>
<head>
    <meta name="x-apple-disable-message-reformatting"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>

        /*@import url('https://fonts.googleapis.com/css?family=Roboto');*/
        @import url('https://fonts.googleapis.com/css?family=Open+Sans');

        * {
            font-family: 'Open Sans', sans-serif;
        }

        .outer-wrapper {
            width: 100%;
            background: rgb(247, 248, 250);
        }

        .wrapper {
            width: 100%;
        }

        .banner {
            width: 100%;
            overflow: hidden;
            border-radius: 2px;
        }

        .banner img {
            width: 148%;
            overflow: hidden;
        }

        .header {
            padding: 8px 16px 8px 16px;
            width: calc(100% - 32px);
            text-align: center;
        }

        .supportive {
            color: rgba(0, 0, 0, 0.58);
        }

        .light-supportive {
            color: rgba(0, 0, 0, 0.38);
        }

        .header h2 {
            font-size: 12px;
            font-weight: 400;
            height: 8px;
        }

        .header h1 {
            font-size: 18px;
            font-weight: 500;
            height: 18px;
        }

        .header h5 {
            font-size: 8px;
            font-weight: 400;
            height: 8px;
        }

        table {
            width: 100%;
            margin-top:16px;
            margin-bottom:16px;
        }

        table tr {
            width: 100%;
        }

        table th {
        }

        th.left, th.right {
            width: calc(100% - 70% - (8px * 3))
        }

        th.card {
            width: calc(70% / 3);
            background: white;
            padding: 6px;
            border-radius: 2px;
            text-align: center;
        }

        th.margins {
            width: 8px;
        }

        .card-title {
            font-size: 6px;
            color: rgba(0, 0, 0, 0.38);
            height: 8px;
            margin: 0 0 4px 0;
            font-weight: 400;
        }

        .card-amount {
            font-size: 12px;
            height: 12px;
            margin: 0 0 6px 0;
            display: inline-block;
        }

        .card-growth {
            font-size: 5px;
            height: 6px;
            margin: 6px 0 0 0;
            font-weight: 400;
            display: inline-block;
        }

        .card-divider{
            height:8px;
        }

        footer{}


        .divider {
            border: none;
            border-bottom: 1px solid rgba(0, 0, 0, 0.12);
            width: calc(100% - 32px);
            margin: 0 auto;
        }

        .green {
            color: #00C853;
        }

        .red {
            color: #FF3D00;
        }

        @media only screen and (min-width: 414px) {
            .card {
                background: blue;
            }
        }

    </style>
</head>
<body style="padding:0;">
<div class="outer-wrapper">

    <div class="wrapper">
        <div class="header">
            <h2 class="supportive">Surf Sharekit</h2>
        </div>

            This is a verification of publishing an item on Surf ShareKit

        <footer>

        </footer>
    </div>
</div>

</body>
</html>